
pipeline {
    agent any

    stages {
        stage('code build') {
            steps {
                node("build_server") {
                    echo "Clone repo"
                    checkout scm
                    echo "Building the war file"
                    sh "mvn install"
                }
            }
        }
    }
}

